# Database configuration
DBHOST HOSTNAME
DBUSER USERNAME
DBPASS PASSWORD
DBNAME nclist

# Required Configuration
BEDFILE sample.bed
OFFSET offsets.txt
WORKDIR temp/
LABEL sample

# Optional Configuration
MEMBERSHIP first
REPORTS no
CONCAT_DUPS yes